	// Add your code here

Difference(vector<int> elements)
{
    this->elements = elements;
}
int computeDifference()
{
     maximumDifference=*max_element(begin(elements),end(elements))-*min_element(begin(elements),end(elements));
        
    
    return maximumDifference;
}
